@model Ros.WebApplication.Models.ViewModels.TeamViewModels.AddUserToTeamViewModel
@{
    ViewBag.Title = "Arne";
}

<h2>Add Users To Teams</h2>

@using (Html.BeginForm())
{

    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <hr />

        @*@Html.DropDownList("Id", (SelectList)Model.UserList, "-- Select Kuk --", htmlAttributes: new { @class = "form-control" })*@

        <div class="form-group">
            @Html.LabelFor(model => model, "Select User", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("Id", (SelectList)ViewBag.UserList, "-- Select User --", htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model, "", new { @class = "text-danger" })
            </div>
        </div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Add" class="btn btn-default" />
                </div>
            </div>

    </div>
}
